vaccinated can still get mild case . i cannot find covid test anywhere due so many unvaccinated getting tested. fact, per dr i saw, there is so much testing that covid tests are running low. they have resort sending out tests which takes few days get